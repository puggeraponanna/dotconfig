# 🚀 Future Todo's & Refinements

## 🛠 Project-Based Layouts (Point 4)
- [ ] Create a `~/.config/tmux/scripts/layouts.sh` script.
- [ ] Logic: Detect `package.json`, `go.mod`, `Cargo.toml`, or `requirements.txt`.
- [ ] Auto-split: If JS project, split bottom and run `npm run dev` or `npm start`.
- [ ] Integration: Hook into `tmux-sess.sh` so every new project session starts with the right windows open.

## ⚡️ Workflow Enhancements
- [ ] **Omni-Switcher**: Merge running tmux sessions into the `Space + e` switcher.
- [ ] **Git Worktree Cleanup**: Add a shortcut to delete worktrees safely.
- [ ] **Config Popups**: A single keybind to open `.zshrc`, `tmux.conf`, or `nvim/init.lua` in a floating scratchpad.

## 🎨 Aesthetic Polish
- [ ] Review any remaining mismatched plugins.
- [ ] Audit contrast for `Atom One Dark` in direct sunlight.
